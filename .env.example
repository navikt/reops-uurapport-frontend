# Environment configuration
APP_ENV=local
VITE_APP_ENV=local

# NAIS configuration (server-side only)
NAIS_CLUSTER_NAME=
NAIS_NAMESPACE=
BACKEND_APP_NAME=

# Wonderwall (for authentication)
VITE_WONDERWALL_INGRESS=

# Next.js client-side environment variables (exposed to browser)
# These are automatically set from the variables above via next.config.js
# NEXT_PUBLIC_APP_ENV (set from APP_ENV/VITE_APP_ENV)
# NEXT_PUBLIC_WONDERWALL_INGRESS (set from VITE_WONDERWALL_INGRESS)

